import { ChatInputCommandInteraction, PermissionFlagsBits, SlashCommandBuilder } from 'discord.js';
import type { Command } from '../../types/command.js';
import { prisma } from '../../services/database.js';
import { getOrCreateUser } from '../../services/userService.js';
import { AppError } from '../../utils/errors.js';
const command: Command = { data: new SlashCommandBuilder().setName('xp').setDescription('Manage member XP').setDefaultMemberPermissions(PermissionFlagsBits.Administrator.toString()).addStringOption(o => o.setName('action').setDescription('XP action').setRequired(true).addChoices({ name: 'Set XP', value: 'set' }, { name: 'Add XP', value: 'add' }, { name: 'Remove XP', value: 'remove' }, { name: 'Set Level', value: 'level' })).addUserOption(o => o.setName('user').setDescription('Member').setRequired(true)).addIntegerOption(o => o.setName('amount').setDescription('XP amount or level').setRequired(true).setMinValue(0)), async execute(interaction: ChatInputCommandInteraction) { if (!interaction.guildId || !interaction.memberPermissions?.has(PermissionFlagsBits.Administrator)) throw new AppError('Only server administrators can manage XP.'); const target = interaction.options.getUser('user', true); const action = interaction.options.getString('action', true); const amount = interaction.options.getInteger('amount', true); const { user } = await getOrCreateUser(target.id, interaction.guildId); const xp = action === 'set' ? amount : action === 'add' ? user.xp + amount : action === 'remove' ? Math.max(0, user.xp - amount) : Math.max(0, (amount - 1) ** 2 * 100); const level = action === 'level' ? amount : Math.max(1, Math.floor(Math.sqrt(xp / 100)) + 1); await prisma.user.update({ where: { id: user.id }, data: { xp, level } }); await interaction.reply({ embeds: [{ title: '✨ XP UPDATED', description: `**${target.username}**\n\n✨ XP: **${xp.toLocaleString()}**\n🏆 Level: **${level}**\n\nAction: **${action}**`, color: 0x5865f2 }] }); } };
export default command;
