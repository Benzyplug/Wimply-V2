import test from 'node:test';import assert from 'node:assert/strict';import { minesRows } from '../../utils/minesComponents.js';test('Mines board is exactly 5x5',()=>{const rows=minesRows('test-session',new Set<number>());assert.equal(rows.length,5);assert.equal(rows.reduce((n,r)=>n+r.components.length,0),25);});test('Mines cashout is a single control inside the board message',()=>{const rows=minesRows('test-session',new Set<number>(),undefined,true);assert.equal(rows[4].components[4].data.custom_id,'mines:cashout:test-session');assert.equal(rows[4].components[4].data.label,'💰');});
